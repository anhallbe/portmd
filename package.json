{
  "name": "portmd",
  "version": "1.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "git clean -fxd ./dist",
    "build": "tsc",
    "build:clean": "npm run clean && npm run build",
    "package": "npm run build:clean && npm pack"
  },
  "author": "Andreas Hallberg",
  "license": "Apache-2.0",
  "bin": {
    "portmd": "./dist/portmd.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "dependencies": {
    "marked": "^0.7.0",
    "puppeteer": "^1.17.0"
  },
  "devDependencies": {
    "@types/marked": "^0.6.5",
    "@types/puppeteer": "^1.12.4"
  }
}
